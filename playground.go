package main

func main() {

	//testing out slices
	// test_slice := make([]int, 0)
	// fmt.Printf("len:%d and cap:%d\n", len(test_slice), cap(test_slice))
	// for i := 0; i < 10000; i++ {
	// 	test_slice = append(test_slice, i)
	// }
	// fmt.Printf("len:%d and cap:%d\n", len(test_slice), cap(test_slice))
	// ptr := &test_slice
	// fmt.Println(&ptr)

}
